import type { Config } from 'vike/types'
import vikeReact from 'vike-react/config'

const config: Config = {
  title: '__PROJECTNAME__',
  description: 'TDesign chat starter powered by React and Vike.',
  extends: [vikeReact],
}

export default config
