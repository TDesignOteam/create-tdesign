import type { Config } from 'vike/types'
import vikeVue from 'vike-vue/config'

const config: Config = {
  title: '__PROJECTNAME__',
  description: 'TDesign starter powered by Vue and Vike.',
  extends: [vikeVue],
}

export default config
